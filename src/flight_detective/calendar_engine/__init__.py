"""Calendar engine (PRD F3): which cultural and holiday windows apply to a departure date.

`gregorian.py` holds the fixed-date windows, `hijri.py` (S06) the drifting Islamic ones,
and `tags.py` (S06) unions them into `tags_for(date)`.
"""
