"""Calendar engine (PRD F3): which cultural and holiday windows apply to a departure date.

`gregorian.py` holds the fixed-date windows, `hijri.py` the drifting Islamic ones,
and `tags.py` unions them into `tags_for(date)`.
"""
